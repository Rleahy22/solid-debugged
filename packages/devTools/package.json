{
  "name": "devtools",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && cp ./src/manifest.json ./dist && cp ./src/devtools-background.html ./dist && cp ./src/devtools-background.js ./dist",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.4.4",
    "vite": "^2.7.2"
  },
  "dependencies": {
    "solid-js": "^1.3.6",
    "vite-plugin-solid": "^2.2.5"
  }
}
